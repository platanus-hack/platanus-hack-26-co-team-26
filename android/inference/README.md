# :android:inference

**Propósito:** LiteRT (TensorFlow Lite), delegados NNAPI/GPU, carga del modelo AIB (.tflite int8). Implementa BiomarkerInferencePort de :core.

**Dueño:** Alex.

**Etiqueta de madurez:** `EXPERIMENTAL` (esqueleto generado, implementación en curso).

Ver `docs/team/DIVISION-DE-TRABAJO.md` y `core/src/commonMain/kotlin/co/helius/core/application/ports/` para el contrato exacto de puertos.
