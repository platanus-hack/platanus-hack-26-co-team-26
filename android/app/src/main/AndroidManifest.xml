<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Transporte adaptativo (Sección 7): BLE siempre encendido durante el incidente -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />
    <!-- Wi-Fi Aware / UWB: android.hardware.wifi.aware, android.hardware.uwb — detección
         en runtime vía PackageManager.hasSystemFeature (Sección 7.1), NO required aquí. -->

    <application
        android:name=".HELIUSApp"
        android:allowBackup="false"
        android:label="HELIUS"
        android:theme="@style/Theme.HELIUS">

        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- READY: WorkManager periódico. ALERT/TRAPPED/RESCUER: foreground service
             persistente (Sección 7.3). Requiere onboarding con exclusión de optimización
             de batería y justificación honesta — ver docs/security/THREAT-MODEL.md. -->
        <service
            android:name=".service.EmergencyForegroundService"
            android:foregroundServiceType="location|connectedDevice"
            android:exported="false" />

    </application>
</manifest>
