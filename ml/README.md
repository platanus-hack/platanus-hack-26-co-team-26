# ml/ — entrenamiento de modelos (Python)

`ppg/` (dataset, preprocesado, entrenamiento, cuantización, export a LiteRT,
model cards) — dueño Alex. `motion/` (features, etiquetado de patrón intencional,
modelos) — dueño Alex. `rf/` (mediciones, ajuste de modelo de propagación) —
dueño Helmut. Ver `docs/architecture/OVERVIEW.md` § 10 (AIB).
