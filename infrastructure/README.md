# infrastructure/ — despliegue

`docker/` (imágenes de servicios), `k8s/` (manifiestos, fase de escalado),
`terraform/` (infraestructura como código), `observability/` (dashboards
Grafana, config de Loki/Tempo). Dueño: Miguel.
