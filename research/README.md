# research/ — notebooks e investigación exploratoria

No es producción. `rf/`, `ppg/`, `motion/`, `localization/`, `validation/` —
resultados que maduran hacia `ml/` o `services/localization` cuando se validan.
