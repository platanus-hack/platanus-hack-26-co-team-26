# services/localization

**Propósito:** Factor graph, ajuste del modelo de propagación RF, heatmaps de verosimilitud, zonas candidatas con confianza (68%/95%), recálculo incremental (Sección 9).

**Dueño:** Miguel.

**Etiqueta de madurez:** `ENGINEERING` (esqueleto generado).

Importa el kernel común de `services/shared/src/api/{domain,application}` — no
duplica entidades ni puertos.
