"""SismoMesh — servicio de personas localizadas.

Hexágono independiente (ADR-0001): `domain` no conoce a nadie, `application` solo
conoce `domain`, `adapters` implementa los puertos y `bootstrap` los cablea.
"""

__version__ = "0.1.0"
