# Motion rules

Animation communicates state, hierarchy, navigation, spatial continuity, or confirmation. The 2D/3D transition lasts 300 ms. Static status icons do not animate continuously. Reduced-motion mode removes nonessential transition duration. Critical content is readable before animation completes.

