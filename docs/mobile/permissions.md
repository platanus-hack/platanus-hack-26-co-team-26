# Permissions

Location is explained before coarse/fine runtime permission. Camera is requested only when starting PPG capture. Bluetooth, notifications, and background location remain separate future permission-center entries. A denied permission produces a safe UI state instead of crashing or silently retrying.

